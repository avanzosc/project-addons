<?xml version="1.0" encoding="UTF-8"?>
<odoo>
<!--  VISTA DE LISTA -->
    <record model="ir.ui.view" id="project_risk_chance_table_tree_view">
        <field name="name">project.risk.chance.table.tree.view</field>
        <field name="model">project.risk.chance.table</field>
        <field name="arch" type="xml">
            <tree>
                <field name="project_id" invisible="context.get('hide_project_id',True)"/>
                <field name="chance"/>
                <field name="chance_id"/>
                <field name="probability_id"/>
                <field name="profit_id"/>
                <field name="chance_level"/>
                <field name="action"/>
                <field name="action_id"/>
                <field name="responsible_id"/>
                <field name="date_due"/>
                
            </tree>
        </field>
    </record>
    <record id="project_risk_chance_table_action" model="ir.actions.act_window">
        <field name="name">Chance Table</field>
        <field name="res_model">project.risk.chance.table</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{
        'hide_project_id': False,
        }</field>
    </record>
</odoo>
