<?xml version="1.0" encoding="UTF-8"?>
<odoo>
<!--  VISTA DE LISTA -->
    <record model="ir.ui.view" id="project_idea_line_viability_tree_view">
        <field name="name">project.idea.line.viability.tree.view</field>
        <field name="model">project.idea.line.viability</field>
        <field name="arch" type="xml">
            <tree editable="top">
                <field name="project_id" invisible="context.get('hide_project_id',True)"/>
                <field name="feasibility_id"/>
                <field name="name"/>
                <field name="percentage"/>
                <field name="line_punctuation"/>
                <field name="weighted_score"/>
                <field name="formula_punctuation"/>
            </tree>
        </field>
    </record>

    <record model="ir.ui.view" id="project_idea_line_viability_form_view">
        <field name="model">project.idea.line.viability</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group name="viability_group">
                        <group name="viability_group_left">
                            <field name="project_id" invisible="context.get('hide_project_id',True)"/>
                            <field name="feasibility_id"/>
                            <field name="name"/>
                        </group>
                        <group name="viability_group_right">
                            <field name="percentage"/>
                            <field name="line_punctuation"/>
                            <field name="weighted_score"/>
                            <field name="formula_punctuation"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record model="ir.ui.view" id="project_idea_line_viability_search_view">
        <field name="model">project.idea.line.viability</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="project_id"/>
                <field name="feasibility_id"/>
                <group name="group_by" expand="0">
                    <filter name="group_project" string="Project"
                            context="{'group_by': 'project_id'}" />
                    <filter name="group_feasibility" string="Category"
                            context="{'group_by': 'feasibility_id'}" />
                </group>
            </search>
        </field>
    </record>

    <record id="project_idea_line_viability_action" model="ir.actions.act_window">
        <field name="name">Viability Lines</field>
        <field name="res_model">project.idea.line.viability</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{
            'hide_project_id': False,
        }</field>
    </record>

    <menuitem id="project_idea_line_viability_menuitem"
              parent="project_idea_menuitem"
              action="project_idea_line_viability_action"
              sequence="20" />
</odoo>
